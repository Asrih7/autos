// Zone JS flags - must be set before zone.js is imported
// Disable requestAnimationFrame patching for better performance
(window as any).__Zone_disable_requestAnimationFrame = true;
// Disable patching of specified event names for better performance
(window as any).__zone_symbol__UNPATCHED_EVENTS = ['scroll', 'mousemove'];
