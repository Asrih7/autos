@Library('jenkins-pipelines') _
angularBuildOcpPipeline([
    area: 'mnv', 
    projectName: 'mnv-autos-ng', 
    compileDir: 'dist/repository', 
    activeTest: true,
    additionalEmails: ['david.sena@helvetia.es'], 
])
