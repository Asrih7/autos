import { ScrollOnRenderDirective } from "./scroll-on-render.directive";

describe("ScrollOnRenderDirective", () => {
  it("should create an instance", () => {
    const directive = new ScrollOnRenderDirective();
    expect(directive).toBeTruthy();
  });
});
